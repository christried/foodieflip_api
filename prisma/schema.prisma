// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

enum RecipeStatus {
  PENDING
  APPROVED
  REJECTED
}

enum UserRole {
  USER
  ADMIN
}

model User {
  id               String   @id @default(cuid())
  googleSub        String   @unique
  email            String   @unique
  username         String?
  usernameCanonical String?  @unique
  role             UserRole @default(USER)
  lastLoginAt      DateTime?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
  submittedRecipes Recipe[] @relation("SubmittedRecipes")
  favoriteRecipes  FavoriteRecipe[]
}

model Recipe {
  id             String       @id @default(cuid())
  title          String
  shortTitle     String       @unique
  time           Int
  imageExtension String @default("jpg")
  imageAlt       String
  ingredients    String[]
  instructions   String[]
  tagsPublic     String[]
  tagsInternal   String[]
  upvotes        Int          @default(0)
  downvotes      Int          @default(0)
  submittedBy    String       @default("FoodieFlip")
  submittedByUserId String?
  submittedByUser User? @relation("SubmittedRecipes", fields: [submittedByUserId], references: [id], onDelete: SetNull)
  favoritedByUsers FavoriteRecipe[]
  status         RecipeStatus @default(PENDING)
  approvedBy     String?
  approvedAt     DateTime?
  reviewNotes    String?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt

  @@index([submittedByUserId])
}

model FavoriteRecipe {
  userId    String
  recipeId  String
  createdAt DateTime @default(now())

  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  recipe Recipe @relation(fields: [recipeId], references: [id], onDelete: Cascade)

  @@id([userId, recipeId])
  @@index([recipeId])
}